---
import { Image } from 'astro:assets';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { linkedin, socials } from '../../data/social';
import headshot from '../../assets/photos/headshot.jpg';

const FORMSPREE_ID = 'xjybydpa';
---

<BaseLayout
	title="Contact"
	description="Get in touch to book Laila Bougria for a keynote or conference talk."
>
	<main class="wrap">
		<section class="page-hero">
			<div class="page-hero-text">
				<h1>Hi, I'm Laila<br />Let's talk about your event</h1>
				<p class="lede">
					Booking a keynote, inviting me for a conference talk, or just want a quote? Tell me
					about your event below, or reach out directly on{' '}
					<a class="inline-link" href={linkedin.url} target="_blank" rel="noopener">LinkedIn</a>.
				</p>
			</div>
			<Image class="headshot" src={headshot} alt="Laila Bougria" width={480} height={480} />
		</section>

		<section class="section" style="border-top: none;">
			<form class="contact-form" action={`https://formspree.io/f/${FORMSPREE_ID}`} method="POST">
				<input type="hidden" name="_subject" value="New speaking inquiry from lailabougria.github.io" />
				<input type="text" name="_gotcha" style="display:none" tabindex="-1" autocomplete="off" />

				<div class="form-field">
					<label for="name">Name</label>
					<input type="text" id="name" name="name" required />
				</div>

				<div class="form-field">
					<label for="email">Email</label>
					<input type="email" id="email" name="email" required />
				</div>

				<div class="form-field">
					<label for="organization">Organization / event</label>
					<input type="text" id="organization" name="organization" />
				</div>

				<div class="form-field">
					<label for="engagement">What are you looking for?</label>
					<select id="engagement" name="engagement">
						<option value="quote-existing-keynote">A quote for an existing keynote</option>
						<option value="quote-new-keynote">A quote for a new keynote</option>
						<option value="talk">A conference talk</option>
						<option value="workshop">A workshop</option>
						<option value="other">Something else</option>
					</select>
				</div>

				<div class="form-field">
					<label for="event-date">Event date (if known)</label>
					<input type="date" id="event-date" name="event_date" />
				</div>

				<div class="form-field">
					<label for="message">Message</label>
					<textarea id="message" name="message" required></textarea>
				</div>

				<button type="submit" class="btn primary">Send</button>
			</form>
		</section>

	</main>
</BaseLayout>

<style>
	.page-hero {
		padding: 76px 0 30px;
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 48px;
	}
	.page-hero-text {
		flex: 1;
		min-width: 0;
	}
	h1 {
		font-family: var(--serif);
		font-weight: 500;
		font-size: clamp(34px, 5vw, 48px);
		margin: 0 0 20px;
	}
	.lede {
		display: block;
		font-size: 17px;
		color: var(--muted);
		max-width: 62ch;
	}
	.inline-link {
		color: var(--accent-2);
		font-weight: 600;
	}

	.page-hero :global(.headshot) {
		width: 280px;
		height: 280px;
		object-fit: cover;
		border-radius: 3px;
		border: 1px solid var(--line);
		flex-shrink: 0;
	}

	.contact-form {
		display: flex;
		flex-direction: column;
		gap: 22px;
		max-width: 520px;
	}
	.contact-form .btn {
		align-self: flex-start;
	}

	@media (max-width: 700px) {
		.page-hero {
			flex-direction: column-reverse;
			align-items: flex-start;
			gap: 24px;
		}
		.page-hero :global(.headshot) {
			width: 180px;
			height: 180px;
		}
	}
</style>